# Django Server URL
# The URL of your SaveNLoad Django server
SAVENLOAD_SERVER_URL=http://localhost:8000

# Note: FTP configuration is done via rclone.conf file
# The rclone.conf file should be in the rclone/ directory
# alongside the rclone.exe executable
# 
# Example rclone.conf content:
# [ftp]
# type = ftp
# host = SERVER_IP
# user = your-ftp-username
# pass = your-ftp-password


# Versioning 0.0.0
# 1. 0 < will use when properly or fully released
# 2. 3. < changes in file processing from ftputil to smb to ftp using rclone
# 3. 46b < 46 commits have been done since the last change of file processing b = beta
APP_VERSION="0.3.46b"